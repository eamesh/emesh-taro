import { View } from '@tarojs/components';
import { defineComponent } from 'vue';

export default defineComponent({
  name: 'Goods',

  setup () {
    //
  },

  render () {
    return (
      <View class='page'>
        goods
      </View>
    );
  }
});
