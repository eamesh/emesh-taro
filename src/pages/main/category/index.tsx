import { View } from '@tarojs/components';
import { defineComponent } from 'vue';

export default defineComponent({
  name: 'Category',

  setup () {
    //
  },

  render () {
    return (
      <View class='page'>
        category
      </View>
    );
  }
});
