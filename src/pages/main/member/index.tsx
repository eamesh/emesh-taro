import { View } from '@tarojs/components';
import { defineComponent } from 'vue';

export default defineComponent({
  name: 'Member',

  setup () {
    //
  },

  render () {
    return (
      <View class='page'>
        member
      </View>
    );
  }
});
