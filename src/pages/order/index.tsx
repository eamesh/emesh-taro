import { View } from '@tarojs/components';
import { defineComponent } from 'vue';

export default defineComponent({
  name: 'Order',

  setup () {
    //
  },

  render () {
    return (
      <View class='page'>
        order
      </View>
    );
  }
});
